# DisasterTweetPrediction
This project uses NLP and ML techniques to identify disaster-related tweets. It preprocesses text, applies TF-IDF and Word2Vec for vectorization, and uses SVM and Naive Bayes models, achieving 80% accuracy. Evaluated with k-fold cross-validation for robust crisis identification.
